import { Database } from 'better-sqlite3';
import { IAsyncStorage } from '../react-native/types';
export interface ReactNativeStorageOptions {
    asyncStorage: IAsyncStorage;
}
export interface NodeJSStorageOptions {
    database: string | Database;
    tableName?: string;
}
export declare type KeyValueStorageOptions = Partial<ReactNativeStorageOptions & NodeJSStorageOptions>;
export declare abstract class IKeyValueStorage {
    abstract getKeys(): Promise<string[]>;
    abstract getEntries<T = any>(): Promise<[string, T][]>;
    abstract getItem<T = any>(key: string): Promise<T | undefined>;
    abstract setItem<T = any>(key: string, value: T): Promise<void>;
    abstract removeItem(key: string): Promise<void>;
}
//# sourceMappingURL=types.d.ts.map